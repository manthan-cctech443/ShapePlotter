#include "Manager.h"

// Entry point of the program
int main() {
    Manager manager;  // Create an instance of the Manager class
    manager.chooseShape();  // Call the method to choose and create a shape
    return 0;  // Return 0 to indicate successful execution
}
